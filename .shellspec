--require spec_helper
--format documentation
# Bastille runs under FreeBSD /bin/sh, so exercise the specs under POSIX sh
# rather than whatever interactive shell the developer happens to use.
--shell sh
