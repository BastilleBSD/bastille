# unit-tests/bootstrap-releasePkgbase

ARG RELEASE

INCLUDE core/bootstrap-release --arg OPTIONS="-p" --arg RELEASE=${RELEASE}
INCLUDE core/destroy-release --arg OPTIONS="-fy" --arg RELEASE=${RELEASE}
